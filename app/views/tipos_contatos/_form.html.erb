<%= form_for(@tipos_contato) do |f| %>
  <% if @tipos_contato.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipos_contato.errors.count, "error") %> prohibited this tipos_contato from being saved:</h2>

      <ul>
      <% @tipos_contato.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descricao %><br>
    <%= f.text_field :descricao %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
